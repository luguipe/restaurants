'use client';
import { useQuery } from "@apollo/client";

const UserSignUpPage = () => {

    // if(loading) return <div> Loading...</div>

    // if(error) return <div> Error...</div>


    return <div> UserSignUp List</div>

};

export default UserSignUpPage;